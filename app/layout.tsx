import type React from "react"
import type { Metadata } from "next"
import { Inter, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"

const _inter = Inter({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Nirali Vaghani | Senior Frontend Developer",
  description:
    "Senior Frontend Developer with 2+ years of experience specializing in React.js, Next.js, and TypeScript. Building scalable web applications with exceptional user experiences.",
  keywords: ["Frontend Developer", "React.js", "Next.js", "TypeScript", "Web Developer", "Nirali Vaghani"],
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
